# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# uv / virtualenv
.venv/

# tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
.tox/
.nox/

# coverage / logs
.coverage
coverage.xml
htmlcov/
*.log

.artifacts/

# Note: uv.lock IS committed. The semantic index + embedding cache live under
# %LOCALAPPDATA%\engram on Windows, $XDG_DATA_HOME/engram on Linux, or
# ~/.engram otherwise, outside the repo.
